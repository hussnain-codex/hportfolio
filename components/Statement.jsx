import Reveal from "@/components/Reveal";

export default function Statement() {
    return (
        <section className="statement">
            <Reveal as="p" type="fade-up" className="statement-text">
                I don't just write code. I build experiences, systems and products that solve real problems.
            </Reveal>
        </section>
    );
}
